# Claude Code Entry

@AGENTS.md

## Claude Code Notes

- Keep this file thin.
- Do not duplicate current project status from `docs/status.md`.
- Do not turn this file into a second rulebook.
- For durable project overview and workflow conventions, read `README.md`.
- For mutable current status and next steps, read `docs/status.md`.
- For task package and execution report conventions, read `tasks/README.md`.
- For documentation governance tasks, use the sibling `ai-skill-hub` repository as a reference only.
- In docs-only tasks, do not modify `src/`, `config/`, `tests/`, `.github/workflows/`, or `data_samples/expected/`.